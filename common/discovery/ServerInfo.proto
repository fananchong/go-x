syntax = "proto3";

package discovery;

message ServerInfo
{
    string external_ip = 1;     // 外网ip（包括端口。格式 ip:port）
    string intranet_ip = 2;     // 内网ip（包括端口。格式 ip:port）
    uint32 weight = 3;          // 权重
}

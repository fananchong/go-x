syntax = "proto3";

package discovery;

message ServerInfo
{
    uint32 type = 1;
    string external_ip = 2;     // 外网ip（包括端口。格式 ip:port）
    string intranet_ip = 3;     // 内网ip（包括端口。格式 ip:port）
    uint32 ordered = 4;         // 权重
}

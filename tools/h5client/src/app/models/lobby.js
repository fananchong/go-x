(function () {
    'use strict';

    module.exports = Lobby;

    function Lobby(user) {
        this.user = user;
    }

    var proto = Lobby.prototype;

    proto.Login = function () {

    };

})();